# Frontend environment sample

# API root URL for the backend (use https://api.example.com/api when deployed)
VITE_API_URL=http://localhost:5000/api

# Optional: error reporting / analytics (uncomment when configured)
# VITE_SENTRY_DSN=
# VITE_POSTHOG_KEY=
# VITE_POSTHOG_HOST=https://app.posthog.com

# Feature flags: comma-separated identifiers consumed by the UI (optional)
# VITE_FEATURE_FLAGS=pricing-experiments,notifications-redesign
